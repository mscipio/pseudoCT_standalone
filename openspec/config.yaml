# OpenSpec Configuration — pseudoct_standalone
# Auto-generated by sdd-init phase (re-init 2026-07-23). Do not edit manually unless you understand the implications.

project:
  name: pseudoct_standalone
  display: pseudo-CT Attenuation Map Pipeline
  language: MATLAB
  stack:
    - MATLAB (SPM8 r6313, deployment-provided, gitignored)
    - FreeSurfer 5.3
    - Java SSH library (ganymed-ssh2-build250)
  execution_paths:
    local: run_pseudo_CT_local.m — editable MATLAB pipeline
    launchpad: run_pseudo_CT_launchpad.m — compiled backend via SSH

artifact_store:
  mode: both
  paths:
    openspec: openspec/
    engram: topic_key sdd/testing-capabilities-pseudoct-standalone

testing:
  framework: none
  strict_tdd: false
  strict_tdd_reason: >
    No formal test assertion framework (MOxUnit, MATLAB xUnit, matlab.unittest.TestCase) detected.
    Project has 25 custom test/verification scripts in scripts/ (smoke tests, seam tests, targeted
    TDD-style scripts, config validation, comparators, env resistance) but no reusable
    red-green-refactor infrastructure.
  ci_present: false
  ci_path: null
  ci_jobs: []
  ci_notes: >
    CI removed in v2.6.3 — private repo cannot provide MATLAB batch license token to GH runners.
    Run `make lint && make test` locally before release.
  linter: mlint (built-in MATLAB), invoked via scripts/run_lint.m or `make lint`
  formatter: none
  type_checker: none
  coverage: none
  test_scripts:
    smoke: scripts/run_smoke_tests.m (110+ checks — file integrity, parse, defaults, policy)
    lint: scripts/run_lint.m (scans src/ + vers/ + entry scripts)
    seam:
      - scripts/test_atlas_seam.m
      - scripts/test_normalization_seam.m
      - scripts/test_cleanup_seam.m
      - scripts/test_spm_root_seam.m
      - scripts/test_pca_seam.m
      - scripts/test_zero_background_seam.m
    targeted_tdd:
      - scripts/test_auto_discover_messages.m (16 assertions, red-green style)
      - scripts/test_profile_env_resistance.m (32/32 env resistance)
    config_validation:
      - scripts/test_spm_preflight.m (14 threat tests)
      - scripts/test_spm_external_config.m (21 config tests)
    ordering:
      - scripts/test_vers_overrides_complete.m
      - scripts/test_vers_overrides_order.m
      - scripts/test_preflight_ordering.m
    comparators:
      - scripts/pct_compare_nifti_exact.m
      - scripts/pct_compare_semantic_mat.m
      - scripts/test_exact_comparators.m
      - scripts/compare_nifti_data.m
      - scripts/compare_hash_strings.m
    e2e:
      - scripts/diff_entrypoint_runs.m
      - scripts/run_tests.m (manual exec harness)
    other:
      - scripts/test_alias_runtime.m
      - scripts/test_deterministic_error_ids.m
      - scripts/test_launchpad_evidence.m

delivery:
  strategy: ask-always
  review_budget_lines: 800
  chain_strategy: unset
