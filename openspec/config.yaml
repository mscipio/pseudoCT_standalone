# OpenSpec Configuration — pseudo_CT_package_new_for_collab
# Auto-generated by sdd-init phase. Do not edit manually unless you understand the implications.

project:
  name: pseudo_CT_package_new_for_collab
  display: pseudo-CT Attenuation Map Pipeline
  language: MATLAB
  stack:
    - MATLAB (SPM8 r6313, bundled)
    - FreeSurfer 5.3
    - Java SSH library (ganymed-ssh2-build250)
  execution_paths:
    local: run_pseudo_CT_local.m — editable MATLAB pipeline
    launchpad: run_pseudo_CT_launchpad.m — compiled backend via SSH

artifact_store:
  mode: both
  paths:
    openspec: openspec/
    engram: topic_key sdd-init-go/pseudo_ct_package_new_for_collab

testing:
  framework: none
  strict_tdd: false
  strict_tdd_reason: >
    No formal test assertion framework (MOxUnit, MATLAB xUnit, matlab.unittest.TestCase) detected.
    Project has custom smoke tests (scripts/run_smoke_tests.m) and one TDD verification script
    (scripts/test_auto_discover_messages.m) but no reusable test infrastructure for red-green-refactor.
  ci_present: true
  ci_path: .github/workflows/ci.yml
  ci_jobs:
    - lint (mlint on src/ + vers/)
    - smoke-test (run_smoke_tests.m)
  linter: mlint (built-in MATLAB), invoked via scripts/run_lint.m
  formatter: none
  type_checker: none
  coverage: none

delivery:
  strategy: ask-always
  review_budget_lines: 800
  chain_strategy: unset
